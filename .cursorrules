# GourdBoat 项目开发规范

## API 安全设计规范

### 🚫 禁止在API参数中传递用户身份标识

**核心原则**：永远不要在API请求参数中传递 `accountId`、`userId`、`bindToken` 等用户身份标识，应该从认证上下文中获取。

#### ❌ 错误示例
```java
// 错误：允许用户在参数中传递accountId
@PostMapping("/update-profile")
public ResponseEntity<?> updateProfile(@RequestBody UpdateProfileRequest request) {
    // 用户可以篡改accountId访问其他用户数据！
    userService.updateProfile(request.accountId(), request.profile());
}

// 错误：在URL路径中暴露用户ID
@GetMapping("/users/{userId}/profile")
public ResponseEntity<?> getUserProfile(@PathVariable String userId) {
    // 用户可以遍历其他用户的ID！
    return userService.getProfile(userId);
}

// 错误：在请求体中传递用户标识
public record UpdateProfileRequest(
    String accountId,  // ❌ 危险！用户可以篡改
    String userId,     // ❌ 危险！
    ProfileData profile
) {}
```

#### ✅ 正确示例
```java
// 正确：从Spring Security上下文获取用户信息
@PostMapping("/update-profile")
public ResponseEntity<?> updateProfile(@RequestBody UpdateProfileRequest request) {
    // 从认证上下文获取当前用户信息
    CurrentUserInfo userInfo = getCurrentUserInfo();
    userService.updateProfile(userInfo.userId(), request.profile());
}

// 正确：操作当前用户的资源
@GetMapping("/profile")  // 隐含的是"我的"资料
public ResponseEntity<?> getMyProfile() {
    CurrentUserInfo userInfo = getCurrentUserInfo();
    return userService.getProfile(userInfo.userId());
}

// 正确：请求体只包含业务数据
public record UpdateProfileRequest(
    ProfileData profile  // ✅ 只包含业务数据
) {}
```

### 🔐 安全获取用户信息的标准模式

#### 1. 创建用户信息提取工具
```java
/**
 * 从Spring Security上下文获取当前用户信息
 */
private CurrentUserInfo getCurrentUserInfo() {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    if (auth == null || !auth.isAuthenticated()) {
        throw new UnauthorizedException("用户未认证");
    }
    
    // 从JWT中提取用户信息
    String token = getCurrentJwtToken();
    Claims claims = jwtTokenProvider.getClaimsFromToken(token);
    
    return new CurrentUserInfo(
        claims.get("accountId", String.class),
        claims.get("userId", String.class),
        claims.get("userStatus", String.class)
    );
}
```

#### 2. 在Controller中使用
```java
@RestController
@RequiredArgsConstructor
public class UserController {
    
    @PostMapping("/change-password")
    public ResponseEntity<?> changePassword(@RequestBody ChangePasswordRequest request) {
        // ✅ 安全：从认证上下文获取用户信息
        CurrentUserInfo userInfo = getCurrentUserInfo();
        
        userService.changePassword(
            userInfo.accountId(),  // 来自JWT，无法篡改
            request.oldPassword(),
            request.newPassword()
        );
        
        return ResponseEntity.ok().build();
    }
}
```

### 🎯 例外情况（需要特别注意）

#### 管理员操作
```java
// ✅ 例外：管理员操作其他用户时可以传递目标用户ID
@PostMapping("/admin/users/{targetUserId}/disable")
@PreAuthorize("hasRole('ADMIN')")  // 必须有明确的权限检查
public ResponseEntity<?> disableUser(@PathVariable String targetUserId) {
    // 获取当前管理员信息
    CurrentUserInfo adminInfo = getCurrentUserInfo();
    
    // 记录管理员操作日志
    auditService.logAdminAction(adminInfo.userId(), "DISABLE_USER", targetUserId);
    
    userService.disableUser(targetUserId);
    return ResponseEntity.ok().build();
}
```

#### 资源关联操作
```java
// ✅ 例外：操作用户拥有的资源时，资源ID可以作为参数
@DeleteMapping("/articles/{articleId}")
public ResponseEntity<?> deleteArticle(@PathVariable String articleId) {
    CurrentUserInfo userInfo = getCurrentUserInfo();
    
    // 验证文章归属
    if (!articleService.isOwner(articleId, userInfo.userId())) {
        throw new ForbiddenException("无权操作此文章");
    }
    
    articleService.delete(articleId);
    return ResponseEntity.ok().build();
}
```

### 📋 检查清单

在编写API时，请检查：

- [ ] ✅ 所有需要用户身份的操作都从认证上下文获取用户信息
- [ ] ✅ 请求参数中不包含 `accountId`、`userId`、`bindToken` 等敏感标识
- [ ] ✅ URL路径中不暴露用户ID（除非是管理员操作且有权限控制）
- [ ] ✅ 所有用户资源操作都验证了归属权限
- [ ] ✅ 管理员操作有明确的权限注解和审计日志

### 🛡️ 安全考虑

1. **水平权限提升防护**：用户无法通过修改参数访问其他用户数据
2. **垂直权限提升防护**：普通用户无法通过参数获得管理员权限
3. **审计友好**：所有敏感操作都基于认证用户，便于审计追踪
4. **符合RESTful原则**：资源操作基于认证主体，语义清晰

### 🔄 重构指导

如果发现现有代码违反此规则：

1. **识别问题**：查找API参数中的用户标识
2. **提取用户信息**：实现 `getCurrentUserInfo()` 方法
3. **验证权限**：确保用户只能操作自己的资源
4. **更新API文档**：说明API基于认证用户操作
5. **添加测试**：验证权限控制正确性

---

## 代码重构和方法抽取原则

### 方法抽取的判断标准

#### ✅ 应该抽取为独立方法的情况：
1. **复杂业务逻辑**：包含多个相关步骤的完整业务操作
2. **有意义的业务单元**：代表一个完整的、可命名的业务概念
3. **足够的复杂度**：通常超过 10-15 行且包含多个逻辑分支
4. **可重用性**：可能在多个地方被调用的逻辑
5. **独立的职责**：有明确单一职责的代码块

#### ❌ 不应该抽取为独立方法的情况：
1. **简单的服务调用**：只是调用一个服务方法加简单检查
2. **简单的验证**：只有一两行的 null 检查或简单验证
3. **简单的对象构造**：只是创建对象和设置几个属性
4. **过于简单的操作**：少于 5-8 行且逻辑简单的代码块
5. **为了抽取而抽取**：没有实际业务意义的代码块

### 重构原则

#### 平衡原则：
- **避免过度抽象**：不是每个代码块都需要抽取为方法
- **避免过度复杂**：单个方法不应该承担太多职责
- **保持自然的业务边界**：方法应该代表有意义的业务操作
- **考虑维护成本**：过度拆分会增加维护负担

#### 方法长度指导：
- **主方法**：20-40 行为宜，超过 50 行考虑拆分
- **私有方法**：10-30 行为宜，有明确的业务含义
- **工具方法**：5-15 行为宜，职责单一

#### 命名原则：
- 方法名应该清晰表达其业务意图
- 避免技术性命名，优先使用业务术语
- 动词开头，表达具体的操作

### 实际案例参考

#### 好的抽取示例：
```java
// 包含查找、验证、创建的完整业务逻辑，值得抽取
private Account findOrCreateAccount(String provider, String configId, OidcAuthResult oidcResult) {
    // 查找现有账号
    // 验证用户信息
    // 创建新账号
}
```

#### 不必要的抽取示例：
```java
// 过于简单，不需要抽取
private void validateUserInfo(OidcAuthResult oidcResult) {
    if (oidcResult.getUserInfo() == null) {
        throw new RuntimeException("用户信息缺失");
    }
}

// 简单的服务调用，不需要抽取
private OidcAuthResult handleAuthorizationCode(...) {
    OidcAuthResult result = service.handle(...);
    if (!result.isSuccess()) { throw ... }
    return result;
}
```

### 代码审查检查点

在代码审查时，检查以下问题：
1. 是否存在过度抽取的方法？
2. 是否存在过于复杂的单一方法？
3. 方法名是否清晰表达业务意图？
4. 抽取的方法是否有实际的业务价值？
5. 代码的可读性是否得到改善？

### 重构时机

- **功能开发完成后**：在功能实现完成后进行适度重构
- **代码审查时**：通过代码审查发现重构机会
- **维护时**：在修复 bug 或添加功能时顺便重构
- **避免过早优化**：不要在功能尚未稳定时过度重构

---

## 其他开发规范

### 日志规范
- 所有 Service、Client、Controller 必须加 @Slf4j 注解
- 关键业务流程必须有 INFO 级别日志
- 异常处理必须有 ERROR 级别日志
- 日志中严禁输出敏感信息

### 命名规范
- 类名：PascalCase
- 方法名：camelCase  
- 常量：UPPER_SNAKE_CASE
- 包名：全小写，与目录一致

### 架构规范
- 采用 domain、application、infrastructure、interfaces 四层架构
- 每层按领域分包
- 遵循单一事实来源（SSOT）原则 