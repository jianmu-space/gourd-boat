# GourdBoat 项目开发规范（精简版）

## 1. 单一事实来源（SSOT）
- 数据库表结构以 db/schema/schema-postgresql.sql 和 db/schema/schema.sql 为唯一事实来源。
- 配置项以 application.yml 及其环境变体为唯一事实来源。
- 组件、类、包重命名时，需全局同步所有引用、注释和文档。

## 2. 分层架构与包结构
- 采用 domain、application、infrastructure、interfaces 四层分层架构。
- 每层下按领域（如 account、oidc、auth）分包，包声明与物理目录严格一致。
- Controller 必须按领域归属分包：
  - 通用认证相关放在 interfaces.auth
  - OIDC 相关放在 interfaces.oidc
- 禁止使用通用 controller 包归集所有 Controller。

## 3. 命名与文件组织
- 类名：PascalCase
- 方法名：camelCase
- 常量：UPPER_SNAKE_CASE
- 包名：全小写，点分隔，且与目录一致。
- Controller 类名应体现领域和用途，如 AuthController、OidcController、OidcProviderManagementController。
- 迁移、重构后如出现空包目录，应及时删除。

## 4. 代码与配置变更
- 任何包、类、配置重命名或迁移，需全局同步修正所有 import 路径和相关引用，确保编译通过。
- 所有 OIDC 相关代码、文档、注释、接口、测试等引用必须保持一致，遵循单一事实来源原则。

## 5. 其他
- 测试、数据、脚本、配置等文件按功能模块归类存放。
- 敏感信息加密、错误处理、扩展性、性能、安全等细节见详细开发文档。

## 6. 日志最佳实践约定（GourdBoat项目）
1. 所有 Service、Client、Controller、异常处理类必须加 @Slf4j 注解。
2. 关键业务流程、外部接口调用、异常分支必须有日志，日志内容需包含业务主键、用户ID、traceId（如有）。
3. 日志分级：
   - DEBUG：详细调试信息（如参数、返回值、流程分支），开发环境开启，生产环境关闭；
   - INFO：关键业务流程、用户操作、外部接口调用成功等；
   - WARN：非致命异常、降级、超时、重试等；
   - ERROR：致命异常、业务失败、外部接口调用失败等。
4. 日志内容应带上下文（如用户ID、请求ID、业务主键等），便于定位。
5. 日志输出应结构化，推荐JSON格式或带明确信息标签。
6. 日志中严禁输出明文密码、token、银行卡号等敏感信息，需脱敏或不打印。
7. 外部接口调用日志：
   - 请求前 log.info 记录请求参数、目标URL；
   - 响应后 log.debug 记录响应内容（可适当截断/脱敏）；
   - 异常时 log.error 记录异常堆栈和上下文。
8. 日志配置参见 application.yml，生产环境建议日志落盘并归档，开发环境可控制台输出。 